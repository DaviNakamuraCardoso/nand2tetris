class ShapeManager {

    field Shape current;

    constructor ShapeManager new(Grid grid) {
        let current = Shape.new(grid);
        return this;
    }

    method boolean render() {
        var Grid g;

        if (~current.render()) {
            let g = current.getGrid();
            do current.dispose();
            do g.update();
            let current = Shape.new(g);
        }

        return;
    }
}
