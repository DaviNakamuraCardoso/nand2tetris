// Writes a function call
char* command_call(char* function_name, int nargs);

// Writes a function definition
char* command_function(char* function_name, int locals);

// Writes a function return
char* command_return(char* return_label); 
