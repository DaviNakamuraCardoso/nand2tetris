// Manages the function-related commands
char* function_manager(char* cmd1, char* cmd2, char* cmd3);

// Writes a function return
char* command_return(void);

// Writes a label
char* branch_manager(char* cmd1, char* cmd2); 
