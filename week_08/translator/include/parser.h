// Parse a filename
char* parse_filename(char* filename);

// Parse the name of a given directory
char* parse_dirname(char* dirname); 

// Parse the next line of Virtual Machine Code
int parse_line(char* buff, char** destination);
