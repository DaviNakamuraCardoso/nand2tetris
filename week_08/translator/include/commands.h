// Adds all commands to the tree
void add_all_commands(TREE* root);

// Manages the arithmetic functions
char* arithmetic_manager(char* cmd);

// Manages static variables
char* static_man(char* cmd1, int value, char* name); 
