/** "And" chip implementation from Nand2Tetris course
/* Website: www.nand2tetris.org  */

CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
        Nand(a=a, b=a, out=nota);  // NOT a
        Nand(a=b, b=b, out=notb);  // NOT b
        Nand(a=nota, b=notb, out=out); // NOT (nota AND notb)
}
